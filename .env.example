# Optional local/OpenAI-compatible enrichment
OPENAI_BASE_URL=http://127.0.0.1:11434/v1
OPENAI_API_KEY=local

# Optional remote enrichment. Never commit a real token.
REPLICATE_API_TOKEN=
# Existing projects may use this alias; the CLI accepts either name.
REPLICATE_API=
