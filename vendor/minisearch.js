/*
 * Development fallback placeholder.
 * `npm ci && npm run build:static` replaces this file in dist/ with the
 * MiniSearch 7.2.0 UMD build. The application retains a deterministic
 * built-in fuzzy search when this placeholder is used.
 */
