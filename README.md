# L-Note

Offline-first, domain-neutral knowledge packs with fuzzy search, linked evidence, personal notes and optional local AI.

This branch temporarily contains a checksummed source archive. The pull-request bootstrap workflow validates, tests and replaces it with the ordinary project tree.
